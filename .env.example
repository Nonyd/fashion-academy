# Database (PostgreSQL via Podman Compose)
# Copy to .env.local and adjust if you change compose.yaml credentials.
DATABASE_URL="postgresql://fashion:fashion_dev_secret@localhost:5432/fashion_academy"

# Optional: pgAdmin (browser)
# PGADMIN_URL=http://localhost:8080
